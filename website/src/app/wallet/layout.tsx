import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Wallet — Check Balance & Send VELX",
  description:
    "Check your VELX balance and send VeloxDAG tokens. Fair launch PoW blockchain — every coin is mined, zero premine.",
};

export { default } from "./page";
